<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.4" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.InMemory" Version="10.0.10" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.14.1" />
    <PackageReference Include="xunit" Version="2.9.3" />
    <PackageReference Include="xunit.runner.visualstudio" Version="3.1.4" />
    <PackageReference Include="Shouldly" Version="4.3.0" />
    <PackageReference Include="NSubstitute" Version="6.0.0" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\Hika.Domain\Hika.Domain.csproj" />
    <ProjectReference Include="..\..\src\Hika.Application\Hika.Application.csproj" />
    <!-- Narrow exception to "Infrastructure is integration-tested only" — OzowHashHelper and
         OzowNotifyVerifier are pure, deterministic logic with no external dependency
         (no DB, no network), so they belong in the fast unit suite like anything else that
         shape, not gated behind Docker/Testcontainers just because of which project they live in. -->
    <ProjectReference Include="..\..\src\Hika.Infrastructure\Hika.Infrastructure.csproj" />
  </ItemGroup>

</Project>
